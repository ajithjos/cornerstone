---
id: arithmetic_fact_fluency
slug: pathway
title: Addition And Subtraction Foundations
subject_id: maths
area_id: arithmetic
status: active
brief_source: docs/authoring/examples/arithmetic-fact-fluency-brief.md
recommended_age_min: 7
recommended_age_max: 10
stage_ids:
  - fact_families_to_10
  - doubles_and_add_10
  - bridge_through_10_addition
  - bridge_through_10_subtraction
playlist_ids:
  - fact_families_to_10
  - doubles_and_add_10
  - bridge_through_10_addition
  - bridge_through_10_subtraction
entry_points:
  age_7: fact_families_to_10
  age_10: fact_families_to_10
---

# Addition And Subtraction Foundations

This route teaches one clear foundation for early arithmetic.

The learner memorises only a small set of anchor facts:

- all addition facts by total from `1` to `10`
- the matching subtraction facts from the same families
- doubles from `1 + 1` to `10 + 10`
- `10 + 1` through `10 + 9`

Everything else in this route is then derived from those anchors by bridging through `10`.

That means the learner is not asked to memorise every fact through `20` as a disconnected list. The learner is taught a small secure base and then shown how to use it.

## Why This Pathway Exists

Many learners can count objects, copy written sums, and still remain slow or unsure when facts are mixed.

The usual problems are simple:

- the learner counts from `1` instead of recalling a known fact
- subtraction is treated as a separate topic rather than the reverse of addition
- `8 + 7` and `15 - 8` feel like new problems instead of facts that can be derived from `10`
- the learner has no short list of anchor facts that can be practised to fluency

This pathway fixes that by teaching a direct order: facts to `10`, doubles, `10 + n`, bridge-through-`10` addition, and bridge-through-`10` subtraction.

## What Secure Progress Looks Like

At the end of this route, the learner should be able to:

- say and use the fact families for totals through `10`
- recall doubles through `20` and `10 + n` facts without delay
- derive addition facts through `20` by making `10`
- derive subtraction facts through `20` by stepping back to `10`
- answer mixed addition and subtraction prompts through `20` without heavy finger counting

For a parent or coach, the visible signs are straightforward:

- less one-by-one counting
- quicker recovery after a mistake
- more explanation such as `I made 10 first` or `I went back to 10`

## Daily Delivery Shape

Use short direct sessions.

- session length: 10 to 15 minutes
- cadence: 4 to 5 sessions per week
- usual order: teach from the lesson note, do a short written page, then run a short mixed drill only after the method is clear

Teach for understanding first, then accuracy, then speed.

## What This Pathway Covers And What It Does Not

This route is only for addition and subtraction foundations through `20`.

It does not try to cover multiplication in the same sequence. For written methods, pair this route later with [Arithmetic Written Methods](../arithmetic-written-methods/pathway.md).

## The Route

### 1. [Fact Families Through 10](./playlists/fact-families-through-10.md)

The learner studies totals `1` to `10` in order and learns to read subtraction from the same family.

What changes here:

- zero is handled correctly as part of the fact set
- totals through `10` stop feeling random
- subtraction facts begin to come from known addition facts

### 2. [Doubles And Add 10](./playlists/doubles-and-add-10.md)

The learner memorises the next anchor facts that are worth knowing cold.

What changes here:

- doubles through `20` become automatic
- `10 + n` facts become immediate
- the learner starts to see a short set of anchors rather than a huge fact list

### 3. [Bridge Through 10 Addition](./playlists/bridge-through-10-addition.md)

The learner uses the earlier anchors to derive addition facts through `20`.

What changes here:

- sums such as `8 + 7` are split into `8 + 2 + 5`
- the learner chooses the part that completes `10`
- mixed addition through `20` becomes more reliable

### 4. [Bridge Through 10 Subtraction](./playlists/bridge-through-10-subtraction.md)

The learner uses `10` as the stepping stone for subtraction through `20`.

What changes here:

- facts such as `15 - 8` are solved by going back to `10` first
- subtraction becomes a structured method instead of backward counting
- mixed subtraction through `20` becomes steadier and quicker

## Entry Guidance

Both the seven-year-old and the ten-year-old entry point start at [Fact Families Through 10](./playlists/fact-families-through-10.md).

An older learner may move faster, but the first check should still confirm that the anchor facts are truly secure.

## How To Move On

Move on when the learner can answer the current stage accurately in mixed order and explain the method in one calm sentence.

Do not move on just because one grouped worksheet looked easy. The aim is usable fluency, not one good page.

## Exit Conditions

This pathway is secure when the learner can:

- answer most mixed addition and subtraction facts through `20` correctly
- explain a bridge-through-`10` method for both addition and subtraction
- use the memorised anchors instead of restarting from `1`
- move into written arithmetic with the facts supporting the work rather than blocking it

## Maintainer Notes

This document describes the route in plain language.

The linked files under `stages/`, `skills/`, `playlists/`, and `materials/` are the authored objects that make the route assignable and testable.

The source planning brief for this route is [docs/authoring/examples/arithmetic-fact-fluency-brief.md](../../../../docs/authoring/examples/arithmetic-fact-fluency-brief.md).